# Caylent's challenge Helm module

## Multi Stage deployment to Kubernetes using Helmfile and Helm Charts

Tools:
- helmfile
- helm charts
- helm secrets


Use:
```
helmfile -e dev apply
helmfile -e stage apply
```

